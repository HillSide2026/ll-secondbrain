# Matter Registry Overrides

This folder contains deterministic override maps for matter resolution.

- `matter_sharepoint_map.yml` maps Clio matter numbers to SharePoint folder IDs/paths when naming conventions are insufficient.
