# =============================================================================
# ll-secondbrain environment variable schema
# =============================================================================
# This file defines ALL required variables. It contains NO real values.
# Copy to .env and populate from the canonical secret store (see docs/ENVIRONMENT.md).
# .env is gitignored and must never be committed.
# =============================================================================

# --- Canva Bridge (OAuth + PKCE) ---
CANVA_CLIENT_ID=
CANVA_CLIENT_SECRET=
CANVA_REDIRECT_URI=http://127.0.0.1:3000/oauth/callback
CANVA_SCOPES=design:content:read design:content:write design:meta:read brandtemplate:meta:read
SESSION_SECRET=
PORT=3000
CANVA_TOKEN_FILE=./tokens.json
CANVA_STATE_FILE=./oauth_state.json

# --- Azure / Microsoft Graph ---
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# --- SharePoint sites ---
SHAREPOINT_LEGALMATTERS_SITE=levinellp.sharepoint.com/sites/LegalMatters
SHAREPOINT_ONBOARDING_SITE=levinellp.sharepoint.com/sites/Documentation

# --- Graph API configuration ---
GRAPH_API_BASE=https://graph.microsoft.com/v1.0

# --- Google OAuth (Gmail integration) ---
GOOGLE_OAUTH_CLIENT_PATH=
GOOGLE_OAUTH_TOKEN_PATH=

# --- Optional runtime configuration ---
LOG_LEVEL=INFO
